#include "main.h"
/**
 * mul - checks for check for a digit (0 through 9)
 * @a: first int
 * @b: second int
 * Return: Always 0
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
