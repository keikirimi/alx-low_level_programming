#include "main.h"
/**
 * main - check the code
 *
 * Return: Always0
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I tear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
